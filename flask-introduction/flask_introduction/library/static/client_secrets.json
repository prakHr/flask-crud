{
  "web": {
    "client_id": "{{ }}",
    "client_secret": "{{ }}",
    "auth_uri": "{{ }}",
    "token_uri": "{{ }}",
    "issuer": "{{ }}",
    "userinfo_uri": "{{ }}",
    "redirect_uris": [
      "{{ }}"
    ]
  }
}
