cd Downloads
cd flask-introduction
source virtual/bin/activate
cd flask_introduction
FLASK_APP=myFlaskApp.py flask run

okta 
https://dev-283049.okta.com
0oa11s9claa99D70r4x6
WN7WxCn6qQKrt4AN72YAIXb7en5pku_3hPJv12QV
(Token Value)00nGaldbmuaeAwci-mQCB-ovbWBTANUYe3bO4l2dWg

|_flask_introduction
  |_myFlaskApp.py(Need to change)
  |_library
    |_ client_secrets.json
    |_ database_extractor.py(Done Need to change)
    |_ _05_database_app_template_eng.py(Done Need to change)
    |_ project1-a9674-firebase-adminsdk-ddztm-263a615578.json
    |_ templates(Need to add template_engine_1.html)
	|_ routing
	   |_ 404.html
	|_ database
	   |_ barcodes_template.html(Need to change)
	   |_ template_engine.html(Need to change)
    

mkvirtualenv --python=/usr/bin/python3.6 flask-introduction
zorroP.pythonanywhere.com

#Go to "green-orb-252313.appspot.com"

# Add the Cloud SDK distribution URI as a package source
echo "deb [signed-by=/usr/share/keyrings/cloud.google.gpg] http://packages.cloud.google.com/apt cloud-sdk main" | sudo tee -a /etc/apt/sources.list.d/google-cloud-sdk.list

# Import the Google Cloud Platform public key
curl https://packages.cloud.google.com/apt/doc/apt-key.gpg | sudo apt-key --keyring /usr/share/keyrings/cloud.google.gpg add -

# Update the package list and install the Cloud SDK
sudo apt-get update && sudo apt-get install google-cloud-sdk

cd Rest_Stuff
google-cloud-sdk/install.sh
google-cloud-sdk/bin/gcloud init
(account: gprakhar0@gmail.com
  disable_usage_reporting: 'False'
  project: green-orb-252313
)
python3 -m pip install virtualenv
virtualenv virtual
source virtual/bin/activate
pip install -t lib -r requirements.txt
gcloud app deploy


git init
git remote remove origin
git remote -v 
git remote add origin 'https://github.com/prakHr/Specialization'
git status
cd Downloads

git add 'Specialization-Tensorflow in Practice'
git commit -m "added useful site addresses and various models"
git push -u origin master
git add 'Specialization-Advanced ML'
git push -u origin master(if not working --force-with-lease)

git rm -r --cached 'Specialization-Advanced ML'(removes from folder as well without cached)
git commit -m "Removed folder from repository"(suppose push failed and some unwanted repositories are added so after here we can remove them )
git push -u origin master


export LDFLAGS="-L/home/linuxbrew/.linuxbrew/opt/isl@0.18/lib"
export CPPFLAGS="-I/home/linuxbrew/.linuxbrew/opt/isl@0.18/include"

sudo apt-get install build-essential
echo 'export PATH="/home/linuxbrew/.linuxbrew/bin:$PATH"' >>~/.profile
echo 'export MANPATH="/home/linuxbrew/.linuxbrew/share/man:$MANPATH"' >>~/.profile
echo 'export INFOPATH="/home/linuxbrew/.linuxbrew/share/info:$INFOPATH"' >>~/.profile
PATH="/home/linuxbrew/.linuxbrew/bin:$PATH"
brew install gcc

https://www.udemy.com/course/python-flask-tutorial-step-by-step
https://keras.io/preprocessing/image/
https://www.tensorflow.org/tutorials/images/transfer_learning
https://www.tensorflow.org/tutorials/text/text_generation
